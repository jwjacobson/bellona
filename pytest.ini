[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
addopts = --cov=src --cov-report=html
env = ENV_FILE=.env.test
markers =
    live: marks tests as live LLM tests (skipped by default; run with --live)